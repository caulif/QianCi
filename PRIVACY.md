# Privacy

潜词尽量把数据留在本地。

## 本地存储

扩展会在浏览器本地保存这些数据：

- 用户词汇画像
- 生词列表
- 熟词列表
- 在线补查后缓存下来的词条

这些数据默认不会同步到云端，也不会自动发送到作者服务器。

## 页面处理

扩展会在用户访问的网页中读取正文文本节点，用于：

- 判断哪些英文单词需要标注
- 响应用户的手动查词操作

默认情况下，这些页面内容只在本地内容脚本中处理，不会被上传到远程服务。

## 联网请求

只有在用户主动触发“联网查询”时，扩展才会向第三方服务发起请求。

当前可能涉及的第三方服务：

- FreeDictionaryAPI
- MyMemory Translation API

发送内容只包含完成查词所需的最小文本，例如：

- 单个英文单词
- 单条英文释义（仅在中文释义缺失时作为翻译回退）

## 权限

扩展当前使用的主要权限包括：

- `storage`：保存本地设置和词表
- `contextMenus`：提供右键查词
- `tabs`：向当前页面发送手动查词消息
- `http://*/*` 与 `https://*/*`：在英文网页中运行内容脚本

## 不做的事

当前版本不会：

- 上传整篇网页内容到作者服务器
- 创建账号体系
- 做云端用户画像
- 接入广告或埋点统计

如果未来隐私行为发生变化，这个文件应该同步更新。
